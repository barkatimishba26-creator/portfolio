# Resume placeholder

Add your resume as `resume.pdf` in this `public/` folder
(i.e. `public/resume.pdf`). The "Download Resume" buttons in the Navbar,
Hero, and Contact sections already link to `/resume.pdf`, so dropping the
file in here is the only step required to make those buttons work.
